jdk:
  - openjdk11

install:
  - ./gradlew -Pversion=${VERSION} :circleprogressbar:publishReleasePublicationToMavenLocal :android-json-form-wizard:publishReleasePublicationToMavenLocal -x test -x lint --no-daemon --stacktrace

